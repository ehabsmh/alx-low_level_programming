#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * strtow - splits a string into words
 * @str: pointer to string
 *
 * Return: double pointer to character
 */

char **strtow(char *str)
{
	if (str == NULL || str == "")
	{
		return (NULL);
	}

	return (str);
}
